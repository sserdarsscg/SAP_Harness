import { create as createOrHandler } from "../../../or.js";
import { create as createErrorHandler } from "../../../error.js";
import { create as createOptionsHandler } from "./options.js";
import { create as createFileHandler } from "./file.js";
import { create as createCacheHandler } from "./cache.js";
export const create = () => createErrorHandler("Failed to read secrets from cache, file or options", createOrHandler("commands.handler.authentication.oauth.secretsProvider", createCacheHandler(), createFileHandler(), createOptionsHandler()));
